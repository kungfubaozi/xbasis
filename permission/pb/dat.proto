syntax = "proto3";

package gs.service.permission;

import "konekko.me/gosion/commons/dto/commons.proto";

service DurationAccess {

    rpc Send (SendRequest) returns (gs.commons.dto.Status);

    rpc Verify (VerifyRequest) returns (VerifyResponse);
}

message VerifyRequest {
    string credential = 1; //凭证
    int64 code = 2;
    string to = 3;
}

message VerifyResponse {
    gs.commons.dto.State state = 1;
    string dat = 2;
}

message SendRequest {
    string to = 1;
    int64 type = 2; //发送方式  40810:weixin  40820:normal
    string credential = 3; //凭证
}
